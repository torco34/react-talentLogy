// import React from "react";

// eslint-disable-next-line react/prop-types
export const TituloTexto = ({ titulo, texto }) => {
  return (
    <div>
      <h1>{titulo}</h1>
      <h2>{texto}</h2>
    </div>
  );
};
